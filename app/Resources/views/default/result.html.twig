{% extends 'layout.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
    {{ parent() }}
    <div>
        <span style="font-size: 120px;">{{ op1 }} + {{ op2 }} = </span><span style="font-size: 120px; color: red">{{ result }}</span>
    </div>
{% endblock %}